# skillpup

`skillpup` is a CLI for storing agent skills in a private git-backed registry and fetching them into a consuming repository.

## Commands

- `skillpup bury init [directory]`
- `skillpup bury add <source-git-url>`
- `skillpup fetch [name[@version]]...`

## Package Management

This repo uses `pnpm`.
